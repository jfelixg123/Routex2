name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  publish_images:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: push image to docker hub
        run: echo "${{ secrets.P_DOCK }}" | docker login -u jeanfg --password-stdin

      - name: build image
        run: docker build -t jeanfg/routex_image:latest .

      - name: Push image
        run: docker push jeanfg/routex_image:latest
        
